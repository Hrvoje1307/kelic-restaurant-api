basePath: /api/v1
definitions:
  models.MenuCategory:
    properties:
      created_at:
        type: string
      description:
        type: string
      id:
        type: string
      name:
        type: string
      sort_order:
        type: integer
    type: object
  models.MenuCategoryInput:
    properties:
      description:
        type: string
      name:
        type: string
      sort_order:
        type: integer
    required:
    - name
    type: object
host: localhost:8080
info:
  contact: {}
  description: Backend API for Kelic Restaurant
  title: Kelic Restaurant API
  version: "1.0"
paths:
  /health:
    get:
      description: Returns 200 if the service is up
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Health check
      tags:
      - health
  /menu/categories:
    get:
      description: Javni endpoint — ne treba autentikaciju
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.MenuCategory'
            type: array
      summary: Dohvati sve kategorije
      tags:
      - Meni
    post:
      consumes:
      - application/json
      parameters:
      - description: Kategorija
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/models.MenuCategoryInput'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.MenuCategory'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Kreiraj kategoriju (admin)
      tags:
      - Meni
  /menu/categories/{id}:
    delete:
      parameters:
      - description: UUID kategorije
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Obriši kategoriju (admin)
      tags:
      - Meni
    put:
      consumes:
      - application/json
      parameters:
      - description: UUID kategorije
        in: path
        name: id
        required: true
        type: string
      - description: Kategorija
        in: body
        name: input
        required: true
        schema:
          $ref: '#/definitions/models.MenuCategoryInput'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.MenuCategory'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Ažuriraj kategoriju (admin)
      tags:
      - Meni
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
